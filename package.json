{
  "name": "backend",
  "version": "1.0.0",
  "description": "aplicación de lista de blogs",
  "license": "ISC",
  "author": "Eleonor Rodríguez",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "test:backend": "cross-env NODE_ENV=test node --test tests/api_blogs.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.1.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "cross-env": "^7.0.3",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.3"
  }
}
